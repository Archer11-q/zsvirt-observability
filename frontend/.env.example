# 前端环境变量（复制为 .env.local 使用，勿提交真实凭据）

# Vite 开发代理目标：后端地址
VITE_PROXY_TARGET=http://localhost:8000

# 可选：后端只读 Bearer Token（与后端 ZSVIRT_OBS_PROBE_TOKEN 一致时启用）
# 留空 = 不发送 Authorization 头（对应后端「认证默认关闭」）
VITE_API_TOKEN=
